/**
 * Constants for game access timer functionality
 */

/** Duration in milliseconds that a game remains accessible (1 hour) */
export const ACCESS_DURATION_MS = 3600000;

/** Prefix for localStorage keys storing game access timestamps */
export const STORAGE_KEY_PREFIX = 'game_access_';

